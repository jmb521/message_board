<%= current_user.username %>
<br />
<br />
<% if current_user.id == @profile.user.id %>
  <% if @profile.address.nil? %>
    <%=link_to "Add your address", new_profile_address_path(@profile) %>
  <% else %>
    <%= link_to "View your address", profile_address_path(@profile.id, @profile.address)%>
    <%= link_to "Edit your address", edit_profile_address_path(@profile.id, @profile.address) %>
  <% end %>
<% end %>
<br />
<br />
Profile Picture:
<br />
<img src="<%= @profile.photo_url %>" />
<br />
<% if current_user.id == @profile.user.id %>
<%= link_to "Edit", edit_profile_path %>
<% end %>
<br />
<br />
